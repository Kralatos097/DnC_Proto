public enum EquipType
{
    Default,
    Self,
    Cible,
}

public enum Action
{
    Default,
    Move,
    Attack,
    Stay,
    Equip,
    CancelMove,
}
public enum StuffSelected
{
    Default,
    EquipOne,
    EquipTwo,
    Consum,
}
